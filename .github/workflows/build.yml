name: Build

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ios-build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Download iOS dependencies
        run: |
          chmod +x ./scripts/setup-ios-deps.sh
          ./scripts/setup-ios-deps.sh

      - name: Download macOS dependencies and merge xcframeworks
        run: |
          chmod +x ./scripts/setup-macos-deps.sh
          ./scripts/setup-macos-deps.sh

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Run iOS unit tests
        run: |
          chmod +x ./scripts/ci-ios-unit-test.sh
          ./scripts/ci-ios-unit-test.sh

      - name: Upload iOS app artifact
        uses: actions/upload-artifact@v4
        with:
          name: ios-simulator-app
          path: build/DerivedData/Build/Products/Debug-iphonesimulator/OfflineTranscription.app
          if-no-files-found: error

  macos-build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Download iOS dependencies
        run: |
          chmod +x ./scripts/setup-ios-deps.sh
          ./scripts/setup-ios-deps.sh

      - name: Download macOS dependencies and merge xcframeworks
        run: |
          chmod +x ./scripts/setup-macos-deps.sh
          ./scripts/setup-macos-deps.sh

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Run macOS unit tests
        run: |
          chmod +x ./scripts/ci-macos-unit-test.sh
          ./scripts/ci-macos-unit-test.sh

      - name: Upload macOS app artifact
        uses: actions/upload-artifact@v4
        with:
          name: macos-app
          path: build/DerivedData/Build/Products/Debug/OfflineTranscriptionMac.app
          if-no-files-found: error
